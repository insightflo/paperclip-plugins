# 호크아이 학습 기록

## 실수 기록
- 2026-03-23: 이슈 코멘트 본문을 비인용 heredoc(`<<EOF`)으로 작성하면서 백틱 내부 명령이 셸에서 치환 실행되었다.
  코멘트/리포트 본문은 항상 단일 인용 heredoc(`<<'EOF'`)으로 작성해 명령 치환을 차단한다.
  관련: [[개발]] (토픽: 셸 heredoc 안전 작성)
- 2026-03-23: Playwright 증거 수집 스크립트를 저장소 루트에서 실행해 `require('playwright')` 모듈 해석 실패가 발생했다.
  `paperclip/` 디렉터리에서 실행하거나 `NODE_PATH`를 명시해 실행 컨텍스트를 고정한다.
  관련: [[개발]] (토픽: Node 모듈 해석 경로)
- 2026-03-23: 수동 재현 스크립트 실행 시 작업 디렉터리를 `~/Projects/webparse`로 고정하지 않아 `./parse` 경로 오류(`no such file or directory`)가 발생했다.
  증거 수집 스크립트 시작 시 `cd`를 명시하고, 명령 직후 `pwd`/실행파일 존재 여부를 확인한다.
  관련: [[개발]] (토픽: CLI 재현 스크립트의 실행 컨텍스트 고정)
- 2026-03-23: `in_review` 상태 이슈에서 `POST /issues/{id}/release`를 호출해 상태가 `todo`로 롤백됐다.
  QA 종료 후에는 `release`를 습관적으로 호출하지 않고, 상태 보존이 필요한 워크플로에서는 `in_review` 전환 후 그대로 종료한다.
  관련: [[감찰관]] (토픽: 상태 게이트 보존), [[cto]] (토픽: 워크플로 API 동작 명세)
- 2026-03-23: QA 실패 핸드오프에서 `assigneeAgentId`를 넣어 버그 이슈를 생성했지만 `tasks:assign` 권한 부족으로 거부됐다.
  이후에는 무할당으로 이슈를 생성하고 코멘트 멘션으로 개발자 핸드오프를 남긴 뒤, `[보고]` 이슈에 할당 필요를 명시한다.
  관련: [[cto]] (토픽: 권한 모델), [[개발]] (토픽: QA 실패 핸드오프)
- 2026-03-23: 확장자 없는 실행 스크립트(`scripts/parse`)를 `importlib.util.spec_from_file_location`로 로드하려다 `NoneType.loader` 오류가 발생했다.
  동일 유형 검증 스텁은 `SourceFileLoader` + `spec_from_loader` 조합으로 로드해 재현 안정성을 확보한다.
  관련: [[개발]] (토픽: 확장자 없는 Python 엔트리포인트 로딩)

## 결정 기록 (ADR)
- 2026-03-23: `parse --url/--render` QA에서 기능 성공만으로 통과 처리하지 않는다.
  오류 경로(stderr)에 Python 스택트레이스가 노출되면 `release_critical_fail`로 집계하고 hard-fail 처리한다.
  관련: [[개발]] (토픽: CLI 오류 메시지 표준화), [[감찰관]] (토픽: 릴리즈 게이트 엄수)
